
- jlsync with custom masks on the command line.

- clean up exclude file list mananagement code

- --report option with bb/hobbit functionality?
- --version command line option.
- pre and post exec command control files?
- log files?
- friendly remote diff/cmp ?
- pager
- use File::Same
- use more rsync options. --update ?  --backup ?


- wrap rsync exec in a single function to capture output and check return codes.
