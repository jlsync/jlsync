#
# jlsync.config config file.
#
# The target client host is the last (right-most) field.
# For a given target host/mask, masks are applied in the order left to right.
# The DEFAULT mask is always applied first.
#
DEFAULT www jas1
DEFAULT proxy jas2
DEFAULT proxy www jas3
